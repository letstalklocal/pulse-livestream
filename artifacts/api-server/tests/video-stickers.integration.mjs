import assert from 'node:assert/strict';
import {randomUUID} from 'node:crypto'; import {createRequire} from 'node:module'; import {fileURLToPath} from 'node:url'; import {unlinkSync} from 'node:fs'; import {build} from 'esbuild';
const dir=fileURLToPath(new URL('..',import.meta.url)),out=`${dir}/tests/.video-stickers-${randomUUID()}.cjs`;
await build({stdin:{contents:"export {default as videos} from './src/routes/creator-videos'; export {pool} from '@workspace/db';",resolveDir:dir},outfile:out,bundle:true,platform:'node',format:'cjs',external:['pg-native'],logLevel:'silent',plugins:[{name:'storage',setup(b){b.onResolve({filter:/\/objectStorage$/},()=>({path:'x',namespace:'x'}));b.onLoad({filter:/.*/,namespace:'x'},()=>({contents:'exports.createPrivateGetUrl=async()=>"https://fixture.invalid"'}));}}]});
const {videos,pool}=createRequire(import.meta.url)(out),prefix=`video-sticker-${randomUUID()}`,host=1890000000+Math.floor(Math.random()*100000),buyer=host+1,id=randomUUID();let pack;
const call=async(path,method,uid,body={},params={})=>{const res={statusCode:200,set(){return this;},status(n){this.statusCode=n;return this;},json(v){this.body=v;return this;}};await videos.stack.find(l=>l.route?.path===path&&l.route.methods[method]).route.stack[0].handle({auth:()=>({userId:uid?`${prefix}-${uid}`:null}),body,params,log:{error(){}}},res);return res;};
try{for(const uid of [host,buyer])await pool.query('insert into users(uid,clerk_id,name) values($1,$2,$3)',[uid,`${prefix}-${uid}`,'Video sticker']);
 await pool.query("insert into creator_videos(id,owner_user_id,bunny_id,filename,status,stickers) values($1,$2,$3,'video.mp4','ready','[]')",[id,host,randomUUID()]);await pool.query('insert into creator_video_settings(owner_user_id,selected_video_id,enabled) values($1,$2,true)',[host,id]);
 pack=(await pool.query("insert into media_packs(owner_user_id,name,coin_price,gift_id) values($1,'Pack',5,'heart') returning id",[host])).rows[0].id;await pool.query("insert into media_pack_items(pack_id,position,object_path,content_type,width,height) values($1,0,'/objects/test','image/jpeg',1,1)",[pack]);
 assert.equal((await call('/creator-videos/:id/stickers','put',host,{stickers:[{kind:'gift',giftId:'rose'},{kind:'pack',giftId:'rose',packId:pack}]},{id})).statusCode,200);
 let listed=await call('/creator-videos/:id/stickers','get',buyer,{}, {id});assert.equal(listed.statusCode,200);assert.equal(listed.body.stickers.length,2);assert.equal(listed.body.stickers[1].price,5);const sticker=listed.body.stickers[1];
 await pool.query('insert into coin_balances(user_id,balance) values($1,20),($2,0)',[buyer,host]);const key=randomUUID();let bought=await call('/creator-videos/:id/stickers/:stickerId/unlock','post',buyer,{packId:pack,expectedPrice:5,idempotencyKey:key},{id,stickerId:sticker.id});if (bought.statusCode !== 200) throw Error(JSON.stringify(bought)); assert.equal(bought.body.balance,15);
 bought=await call('/creator-videos/:id/stickers/:stickerId/unlock','post',buyer,{packId:pack,expectedPrice:5,idempotencyKey:key},{id,stickerId:sticker.id});assert.equal(bought.statusCode,200);const counts=await pool.query('select (select count(*) from media_pack_purchases where pack_id=$1) as purchases,(select count(*) from creator_video_gifts where video_id=$2) as gifts,(select count(*) from direct_messages where media_pack_id=$1) as receipts',[pack,id]);assert.deepEqual(counts.rows[0],{purchases:'1',gifts:'1',receipts:'1'});
 assert.equal((await call('/creator-videos/:id/stickers/:stickerId/unlock','post',buyer,{packId:pack,expectedPrice:1,idempotencyKey:randomUUID()},{id,stickerId:sticker.id})).statusCode,200,'owned users view instead of repaying');
 assert.equal((await call('/creator-videos/:id/stickers','put',buyer,{stickers:[]},{id})).statusCode,404);
 console.log('PASS: video sticker configuration/metadata, gift-price pack offer, exact-once purchase, wallet credit, one DM receipt, ownership and video earnings attribution.');
}finally{await pool.query('delete from coin_balances where user_id=any($1::int[])',[[host,buyer]]);await pool.query('delete from users where uid=any($1::int[])',[[host,buyer]]);await pool.end();unlinkSync(out);}

process.exit(0);
